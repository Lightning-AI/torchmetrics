# NOTE: the upper bound for the package version is only set for CI stability, and it is dropped while installing this package
#  in case you want to preserve/enforce restrictions on the latest compatible version, add "strict" as an in-line comment

pypesq @ https://github.com/Lightning-Sandbox/python-pesq/archive/refs/heads/setup/numpy.zip
mir-eval >=0.6, <=0.7
speechmetrics @ https://github.com/Lightning-Sandbox/speechmetrics/archive/refs/heads/master.zip
fast-bss-eval >=0.1.0, <0.1.5
torch_complex <=0.4.3  # needed for fast-bss-eval
