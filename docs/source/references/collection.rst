.. role:: hidden
    :class: hidden-section

#############################
torchmetrics.MetricCollection
#############################

The `MetricCollection` class is a container for other metrics. It is used to combine multiple metrics into one object
that can be updated and computed at once.

.. autoclass:: torchmetrics.MetricCollection
    :noindex:
